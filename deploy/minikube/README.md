# Legacy: Minikube (Redis stack)

These manifests target the **Redis**-based job stack and are kept for reference only.

For the current **Kafka + Postgres** stack, use:

- **Local / demo:** [deploy/demo/](../demo/) (Docker Compose)
- **Cluster:** [deploy/kubernetes/](../kubernetes/) (Kubernetes manifests)
